LAB_DATABASE_URL=sqlite:///labagent.db
# Set a private token when exposing the API beyond localhost.
LAB_API_TOKEN=
# Optional reviewed model planning. No hardware tools are given to the model.
# Select provider/model in the browser workbench. Ollama uses http://127.0.0.1:11434.
# Cloud adapters remain reserved until their API integrations are added.
OPENAI_API_KEY=
